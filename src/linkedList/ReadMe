**** SOME BASIC OPERATIONS ****

- For this directory, a copy of the linkedList structure was implemented

OPERATIONS:
1° "addFirst(x)" -> Inserts an element at the beginning of the list. - CHECK
2° "addLast(x)" -> Inserts an element at the end of the list. - CHECK
3° "removeFirst()" -> Removes and returns the first element of the list.
4° "removeLast()" -> Removes and returns the last element of the list.
5° "get(index)" -> Returns the element at the specified position in the list.
6° "getFirst()" -> Returns the first element without removing it.
7° "getLast()" -> Returns the last element without removing it.
8° "remove(x)" -> Removes and returns the element at the specified index.
9° "remove(x)" -> Removes the first occurrence of the specified value from the list.
10° "contains(x)" -> Checks whether the list contains the specified element.
11° "size()" -> Returns the number of elements in the list.
12° "isEmpty()" -> Returns true if the list contains no elements.
13° "clear()" -> Removes all elements from the list.

Learning goals:


AI-assisted development:

